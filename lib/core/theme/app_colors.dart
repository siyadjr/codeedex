import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// App surfaces
  static const Color appBackground = Color(0xFFF2F2F2);
  static const Color surfaceWhite = Color(0xFFFFFFFF);

  /// Brand & primary actions
  static const Color primary = Color(0xFF7C2F02);

  /// Text & icons
  static const Color textPrimary = Color(0xFF222222);

  /// Highlights & emphasis
  static const Color highlight = Color(0xFFFFAD70);

  /// Soft accents (chips, dividers, tags)
  static const Color softAccent = Color(0xFFCFB9FF);
}
